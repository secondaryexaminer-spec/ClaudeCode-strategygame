# 下一阶段规划

地基重构与模拟器升级已收尾（见 [`src/README.md`](src/README.md) 与
[`sim/README.md`](sim/README.md)）。这份文档记录接下来做什么、按什么顺序、
凭什么这么排。

## 现状：结构干净了，但对局本身不好看

三组实测数字（取自 `sim/baseline.json` 的 36 局，不是估计）：

| 指标 | 数值 | 说明 |
|---|---|---|
| 打满回合上限 | **26 / 36 局（72%）** | 靠"数城判定"分胜负，不是真打完 |
| 纯陆图打满率 | 5 / 10（50%） | `heartland-2ai` |
| 含海图打满率 | 21 / 26（81%） | 其余五个场景合计 |
| 平均 stall 次数 | 每局约 832 次 | 单位连续 2 回合没动且目标是据点 |

打满的那 26 局里，有多局是"10 城 / 17 据点"这种压倒性领先却仍然收不了尾。
**优势方无法把优势转化成胜利**，这是体验上最明显的问题，也是下面 A 项的依据。

> `reserves` 恒为 0 **不是 bug**：桥头预备队分支要求 `profile.agg === 'cautious'`，
> 而诊断跑的是 `balanced`，本来就不该触发。别把它当成死代码删掉。

## 四个方向

### A. AI 终结能力（体验收益最大）

- **依据**：上表。含海图 81% 打满，纯陆图 50% —— 差距指向跨海攻坚与滩头僵持。
- **动哪里**：`ai/intent.js`（战略取舍）、`ai/turnloop.js`（分支顺序）。
  `ai/decide.js` 只负责执行，一般不用动。
- **风险**：**必然改变行为基线**。改完 `verify` 会红，这是预期的，要逐条确认
  差异符合预期后再 `npm run snap`，并在提交信息里写明原因。
- **工作量**：大，且需要反复调参。

### B. `shuffle` 换 Fisher-Yates（技术债，小而明确）

- **现状**：`shuffle = items => [...items].sort(() => Math.random() - 0.5)`,
  分布不均，且依赖引擎的排序实现。
- **风险**：随机数消耗顺序变化 → 基线整体失效。
- **工作量**：改一行 + 重新 `snap` + 跑 `sim:suite` 确认平衡没漂移。

### C. 数据模型（MapDefinition / SaveState / RulesConfig）—— ✅ 已完成

2026-08-12 完成，四个模块：

| 模块 | 做了什么 |
|---|---|
| `core/mapdefs.js` | 12 张地图改成 steps 数据，`mapgen` 退化成解释器 |
| `core/rules.js` | 可调参数的来源/类型/默认值/范围，读取源可替换 |
| `io/savestate.js` | 存档字段、版本号、迁移链、入口校验 |
| `io/input-backend.js` | 事件 → 画布像素，合并三处重复换算 |

顺带修掉一个真实隐患：`newGame` 里配置有**两条读取路径**（地形直读 DOM、
settings 走另一条），改坏配置读取竟然 `verify` 全绿。统一后同样的改坏产生
382 处差异。

### D. 补剩余测试盲区

- 面板按钮里还没验"挂对了"的：升级据点、修整驻军、工程师建营地、运兵船预载
  下拉、统计翻页、暂停菜单、结算弹窗。
- 导入存档（需要给 `FileReader` 打桩）。
- 观战分支、同格叠放的循环切换、contextmenu 抑制。
- **工作量**：小到中，且每条都能套用现成的 `dispatchOn` 模式。

### E. 已知但暂不处理的债

- `panBy` 对 dx 和 dy 都乘了 x 方向的缩放比（`canvas.width / rect.width`），
  画布非等比拉伸时纵向拖拽会偏。抽 InputBackend 时**照搬未改**并标注了注释
  —— 修它会改变行为，要单独一次提交。

## 建议顺序：B → 建质量指标 → A → D

**1. 先做 B，单独结清。**
B 和 A 都会让基线失效。让它们分两次发生，每次只有一个原因，差异才可归因；
合在一起做，`verify` 红了会分不清是 shuffle 换算法还是 AI 改了策略。
B 小、独立、差异可以完全理解，适合先清掉。

**2. 做 A 之前，先把质量指标做成工具。**
现在"72% 打满"是手工从 `baseline.json` 里统计出来的。A 是个要反复调参的活，
每调一次都手工统计不现实。所以先给 `sim/` 加一个质量报告，至少输出：

- 打满上限的比例（**主目标**，期望显著低于 72%）
- 平均回合数、结束方式分布（真打完 vs 数城判定）
- 每局 stall 次数

有了这个，A 的每次改动都有可测量的目标函数，而不是凭感觉调。这也符合项目
既有的做法：先有度量，再动代码。

**3. 然后 A。**
体验收益最大，但工作量也最大，放在有度量之后做。

**4. D 排在最后。**
现有的网已经能兜住重构类改动（36 局逐字段 + 11 条交互链 + 108 次地形自检），
剩下那些盲区都不在主路径上。

## 第一步具体怎么着手

```bash
npm run verify          # 确认起点是绿的
# 改 shuffle 为 Fisher-Yates（一处，注意 Math.random 必须晚绑定）
npm run verify          # 预期变红 —— 确认差异只来自随机序变化
npm run snap            # 确认无误后更新基线
npm run sim:suite       # 胜率是否仍在合理区间
git commit              # 提交信息里写明"基线已更新，原因是 shuffle 换算法"
```

⚠️ 这是**唯一**允许在 `verify` 变红后跑 `snap` 的情形：改动本就打算改变行为。
纯结构重构时变红就是搬错了，去改代码，不要动基线（见 `sim/README.md` 的铁律）。
